AFTER FIXING A DOCUMENT, write it as PDF file to the doc directory,
as "InstructionsGoal.pdf". And of course you need to run the build script to
transport the files to the zip files and GOAL release.


The top level documents are InstructionsGoal, InstructionsGoalSimple, and InstructionsJava


how to insert links to other files in openoffice:

menu Insert/Section


 * Check the Link box
 * Type MANUALLY file:<filename>. Make sure that included file is in same dir as your docu including the odt extension.
 * or use browse button

To EDIT links
 * put cursor in linked section
 * Menu Format/Section
 
 SAVE PDF
  * use Print and then save as pdf. Exporting directly as PDF from openoffice results in mangled eps pictures.